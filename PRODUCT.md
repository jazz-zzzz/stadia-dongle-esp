# Product

## Register

product

## Users

Stadia 手柄用户和固件维护者。他们通常在 Windows 电脑上连接 ESP32-S3 接收器，偶尔需要配对手柄、映射 Assistant/Capture、查看输入诊断或恢复设备。配置应当是短任务，不应要求用户改变日常网络或长期运行后台程序。

## Product Purpose

让 Stadia 手柄作为低延迟、带震动的 Xbox 360 兼容无线手柄继续工作，并提供完全本地的设备管理界面。USB WebHID 是首选配置通道，现有 Wi-Fi AP/HTTP 页面保留为首次设置和恢复入口。成功意味着用户能看清当前连接通道和设备状态，完成配置后立即回到游戏，不需要云账户、驱动或重复烧录。

## Brand Personality

工程化、克制、透明。界面像可靠的硬件控制面板：状态真实、措辞直接、错误可操作，不用营销语言掩盖限制。

## Anti-references

- 不做依赖账户、云同步或遥测的设备管理门户。
- 不把连接接收器热点作为唯一配置方式。
- 不要求安装常驻 Windows 服务或专用驱动。
- 不做霓虹电竞装饰、夸张动效或信息密度低的营销卡片。
- 不隐藏固件升级、断连、浏览器兼容性和破坏性操作的真实后果。

## Design Principles

1. 连接状态优先：始终明确显示 USB、Wi-Fi、未连接或断连状态。
2. 一个概念模型：HTTP 和 WebHID 共享相同的状态、配对、控制器和映射界面。
3. 本地优先：硬件数据和配置只在浏览器与接收器之间传输。
4. 可恢复：Wi-Fi 配置入口保留，USB 失败时提供明确回退路径。
5. 诊断可见：保留原始报告、摇杆、扳机、电量和 USB 状态。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为基线。所有操作支持键盘，焦点清晰，状态不只依赖颜色，错误文本说明恢复办法；尊重减少动态效果设置。布局继续支持窄屏，但主要使用场景是 Windows 桌面版 Chrome 或 Edge。

